import './styles.css';

function AddItem(setItems) {
    return (
      <div className="AddItem">
        <header className="AddItem-header">
          
        </header>
      </div>
    );
  }

export default AddItem;